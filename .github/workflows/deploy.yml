name: Azure Python Function Deploy

on:
  push:
    branches:
      - main

env:
  RESOURCE_GROUP_NAME: azure-cloud-resume-rg-c10c3c55
  LOCATION: "Canada Central"
  STORAGE_ACCOUNT_NAME: crcstoragec10c3c55
  FUNCTION_APP_NAME: crc-visitor-func-c10c3c55
  PLAN_NAME: crc-func-plan-c10c3c55
  COSMOSDB_ACCOUNT_NAME: crccosmosc10c3c55
  COSMOS_DB_NAME: cloudresume
  COSMOS_CONTAINER_NAME: visitorcounter

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Azure Login
        uses: azure/login@v1
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      # Ensure resource group exists
      - name: Ensure Resource Group exists
        run: |
          if ! az group show --name $RESOURCE_GROUP_NAME &> /dev/null; then
            az group create --name $RESOURCE_GROUP_NAME --location "$LOCATION"
          fi

      # Ensure Storage Account exists
      - name: Ensure Storage Account exists
        run: |
          if ! az storage account show --name $STORAGE_ACCOUNT_NAME --resource-group $RESOURCE_GROUP_NAME &> /dev/null; then
            az storage account create \
              --name $STORAGE_ACCOUNT_NAME \
              --resource-group $RESOURCE_GROUP_NAME \
              --location "$LOCATION" \
              --sku Standard_LRS
          fi

      # Ensure Cosmos DB exists
      - name: Ensure Cosmos DB exists
        run: |
          if ! az cosmosdb show --name $COSMOSDB_ACCOUNT_NAME --resource-group $RESOURCE_GROUP_NAME &> /dev/null; then
            az cosmosdb create --name $COSMOSDB_ACCOUNT_NAME --resource-group $RESOURCE_GROUP_NAME --locations regionName="$LOCATION" --kind GlobalDocumentDB
          fi

      # Get Cosmos DB connection string
      - name: Set Cosmos DB Connection String
        id: cosmos
        run: |
          echo "COSMOS_CONNECTION_STRING=$(az cosmosdb keys list \
            --name $COSMOSDB_ACCOUNT_NAME \
            --resource-group $RESOURCE_GROUP_NAME \
            --type connection-strings \
            --query 'connectionStrings[0].connectionString' -o tsv)" >> $GITHUB_ENV

      # Create Cosmos DB database and container
      - name: Create Cosmos DB Database & Container
        run: |
          az cosmosdb sql database create \
            --account-name $COSMOSDB_ACCOUNT_NAME \
            --resource-group $RESOURCE_GROUP_NAME \
            --name $COSMOS_DB_NAME
          az cosmosdb sql container create \
            --account-name $COSMOSDB_ACCOUNT_NAME \
            --database-name $COSMOS_DB_NAME \
            --name $COSMOS_CONTAINER_NAME \
            --partition-key-path "/id"

      # Ensure Function App Plan exists
      - name: Ensure Function App Plan exists
        run: |
          if ! az functionapp plan show --name $PLAN_NAME --resource-group $RESOURCE_GROUP_NAME &> /dev/null; then
            az functionapp plan create \
              --name $PLAN_NAME \
              --resource-group $RESOURCE_GROUP_NAME \
              --location "$LOCATION" \
              --sku Y1 \
              --is-linux
          fi

      # Ensure Function App exists
      - name: Ensure Function App exists
        run: |
          if ! az functionapp show --name $FUNCTION_APP_NAME --resource-group $RESOURCE_GROUP_NAME &> /dev/null; then
            az functionapp create \
              --name $FUNCTION_APP_NAME \
              --storage-account $STORAGE_ACCOUNT_NAME \
              --resource-group $RESOURCE_GROUP_NAME \
              --plan $PLAN_NAME \
              --runtime python \
              --runtime-version 3.11 \
              --functions-version 4 \
              --os-type Linux
          fi

      # Set Function App settings
      - name: Configure Function App settings
        run: |
          az functionapp config appsettings set \
            --name $FUNCTION_APP_NAME \
            --resource-group $RESOURCE_GROUP_NAME \
            --settings FUNCTIONS_WORKER_RUNTIME=python \
                        COSMOS_CONNECTION_STRING=$COSMOS_CONNECTION_STRING \
                        COSMOS_DB_NAME=$COSMOS_DB_NAME \
                        COSMOS_CONTAINER_NAME=$COSMOS_CONTAINER_NAME

      # Deploy Function App code
      - name: Deploy Function App code
        run: |
          cd function_app
          zip -r ../function_app.zip .
          az functionapp deployment source config-zip \
            --name $FUNCTION_APP_NAME \
            --resource-group $RESOURCE_GROUP_NAME \
            --src ../function_app.zip

      # Output Visitor Counter URL
      - name: Show Visitor Counter URL
        run: |
          echo "✅ Visitor Counter URL: https://$FUNCTION_APP_NAME.azurewebsites.net/api/visitorcounter"
